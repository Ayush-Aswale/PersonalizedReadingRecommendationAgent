[project]
name = "personalized-reading-recommendation-agent"
version = "1.0.0"
requires-python = ">=3.11"

[tool.vercel]
entrypoint = "api.app:app"

[tool.vercel.scripts]
build = "cd frontend && npm install && npm run build"
